import styled from 'styled-components';

export const WishlistWrapperContainer = styled.div`
  > * {
    margin: 0;
  }
`;

export const MainSection = styled.div`
  background-color: var(--color-card-divider);
`;
